services:
  marketing-mcp:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - marketing_mcp_data:/var/lib/marketing-mcp
    environment:
      MARKETING_MCP_HOST: 0.0.0.0
      MARKETING_MCP_PORT: 8000
volumes:
  marketing_mcp_data:
