# Baseline Evidence — v0.4.0 at Current Head

Purpose: record what the repository actually does today, before stabilization edits. This file is a
baseline record, not a release claim. Nothing here marks a gate green.

## Identity

| Field | Value |
|---|---|
| Commit SHA (all commands below) | `8f7e2a9a03b4d0d580e43eb21270e917953e0b11` |
| Branch | `main` (PR #1 `plan/production-grade-stabilization` merged) |
| Working tree at capture time | clean |
| Platform | macOS (darwin, arm64) |
| Package version (`pyproject.toml`) | `0.4.0` |
| Runtime version (`marketing_mcp.__version__`) | `0.4.0` |
| uv | 0.11.31 |

## Environment

| Package | Installed version |
|---|---|
| Python | 3.12.13 |
| pymc-marketing | 1.0.0 |
| pymc | 6.0.1 |
| pytensor | 3.0.7 |
| arviz | 1.3.0 |
| mcp | 2.0.0 |
| xarray | 2026.7.0 |
| h5netcdf | 1.8.1 |
| pydantic | 2.12.5 |
| numpy | 2.4.6 |
| pandas | 2.3.3 |
| starlette | 1.6.0 |

## Executed commands

| Command | Result |
|---|---|
| `uv sync --frozen --extra dev` | exit 0, 99 packages checked |
| `uv run pytest -n auto -q` (run 1) | **1 failed, 144 passed** in 115.86s |
| `uv run pytest -n auto -q` (run 2) | **145 passed** in 77.23s |
| `uv run pytest -m statistical -q` | 5 passed, 140 deselected in 91.93s |
| `uv run pytest tests/integration/test_mcp_protocol.py -q` | 2 passed in 7.40s (stdio + streamable HTTP) |
| `uv run ruff check src tests` | All checks passed |
| `uv build` | exit 0, wheel + sdist built |
| `uv run marketing-mcp-demo --fast` | exit 0, completed step 10 |

## Test inventory

| Selection | Count |
|---|---|
| Total collected | 145 |
| `-m statistical` | 5 |
| `-m mcp` | 0 (marker declared but unused) |
| Skipped | 0 |

## Build artifacts

| Artifact | SHA-256 |
|---|---|
| `dist/pymc_marketing_mcp-0.4.0-py3-none-any.whl` (66,567 B) | `d85371739d1a3bfbebdf1ec37c808b69c9cfcd2477f5f50745a635e7d3051b76` |
| `dist/pymc_marketing_mcp-0.4.0.tar.gz` (39,512,719 B) | `06f24a3ace7b5b7bf86f0165b8557b339ee082317c1ce978cf8eacdee83db236` |

Hashes are reproducible only for the same commit and build environment; they are recorded for
identity comparison, not as a reproducibility claim.

## Known warnings (not failures)

- `arviz_stats/base/diagnostics.py`: `RuntimeWarning: invalid value encountered in scalar divide`
  (R-hat / MCSE computation on very short chains).
- `multiprocessing/popen_fork.py`: `DeprecationWarning: This process is multi-threaded, use of
  fork() may lead to deadlocks in the child.`
- `pymc_marketing/mmm/time_slice_cross_validation.py:342`: xarray `FutureWarning` about the
  `join` default changing from `outer` to `exact`. This is an upstream deprecation that will become
  a `ValueError`; it affects `cross_validate_mmm`.
- `NumbaWarning: Cannot cache compiled function "numba_funcified_fgraph"`.

## Baseline defects and drift observed

Classified per the stabilization rules: **A** = pre-existing defect, **C** = flaky/non-deterministic.

| ID | Class | Observation | Owner task |
|---|---|---|---|
| B1 | C | `tests/integration/test_mcp_protocol.py::test_mcp_streamable_http_roundtrip` failed in full-suite run 1 under `-n auto` and passed in run 2, in isolation, and when the integration directory ran alone. Non-deterministic under parallel load. | observability/CI plan (flake quarantine) |
| B2 | A | `src/marketing_mcp/cli.py:56` hardcodes `"version": "0.4.0"` in the health response instead of deriving it from `marketing_mcp.__version__`. | production-truth Task 1 |
| B3 | A | `scripts/deploy_cloud_run.sh:14` defaults `IMAGE_TAG` to `v0.5.0`, a version that does not exist. `dashboard/src/App.tsx:26` prints `v0.5.0`. Three independent version literals disagree with the package version. | production-truth Tasks 1, 5 |
| B4 | A | `README.md` described the project as the `0.3.0` release while the package is `0.4.0`; `docs/TOOL-CONTRACTS.md` is titled v0.4.0. Version references are hand-maintained. | production-truth Tasks 1, 4 |
| B5 | A | `AGENTS.md` documents `uv run pytest` as "fast tests (skips statistical marker)", but no `addopts` deselects the marker, so the default run executes real sampling. It also documents `uv run pytest -m mcp`, which selects zero tests. | production-truth Task 4 |
| B6 | A | The sdist contains `dashboard/node_modules` (1,719 entries, 39 MB of the 39.5 MB archive). No `hatch.build.targets.sdist` include/exclude is configured. | production-truth Task 5 |

## Explicitly not verified at this commit

The following were **not** executed here and must not be described as verified:

- Docker image build and container smoke test.
- Cloud Run deployment.
- HTTP authentication behavior beyond `tests/unit/test_auth.py` and
  `tests/integration/test_mcp_auth_http.py` as run inside the fast suite.
- Restart recovery beyond `tests/integration/test_persistence_lifecycle.py`.
- Any capability listed in `CHANGELOG.md` v0.4.0 whose statistical semantics are not covered by the
  5 tests under `tests/statistical/`.
